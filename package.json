{
  "name": "dry-silence-ff52",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "wrangler": "^3.0.0"
  },
  "jest": {
    "moduleNameMapper": {
      "^~(.*)$": "<rootDir>/src/$1"
    }
  }
}
